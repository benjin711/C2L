# @package _global_
---
defaults:
    - override /datasets: lccnet_like
    - override /dataloaders: dl_config1

# General Parameters
general:
    exp_name: debug
    run_mode: train  # train, test
    seed: 1
    log_path: /home/benjin/debug
